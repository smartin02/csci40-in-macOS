{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Compile and Run in External Terminal",
            "type": "shell",
            "command": "${workspaceFolder}/run.sh",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": [],
            "detail": "Compiles all C++ files and runs the executable in an external terminal"
        }
    ]
}
